---
export interface Props {
    name: string;
}

const { name } = Astro.props as Props;
const images = import.meta.glob("/src/assets/icons/**/*", {
    query: "?raw",
    import: "default",
});
const html = await images[`/src/assets/icons/${name}.svg`]!();
---

<Fragment set:html={html} />
