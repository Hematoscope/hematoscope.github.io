---
export interface Props {
    name: string;
}

const { name } = Astro.props as Props;
const { default: innerHTML } = await import(
    /* @vite-ignore */
    `/src/assets/icons/${name}.svg?raw`
);
---

<Fragment set:html={innerHTML} />
