---
import { Image } from "astro:assets";
import appMask from "~src/assets/images/home/app-mask.png"; // https://mockuper.net/generator/mockup/2286/imac
import appLabelings from "~src/assets/images/home/app-labelings.png"; // https://mockuper.net/generator/mockup/2085/imac
import microscope from "~src/assets/images/home/microscope.jpg";
import server from "~src/assets/images/home/server.jpg";

import { WIDTHS } from "~src/consts";
import Section from "~src/components/Section.astro";
import H from "~src/components/HeadingLink.astro";
import LinkButton from "~src/components/LinkButton.astro";
import GlassFrame from "~src/components/GlassFrame.astro";
import Layout from "~src/layouts/Layout.astro";
import SectionCard from "~src/components/SectionCard.astro";
---

<Layout
  title="Cellbytes"
  description="AI solutions for hematopathologists, hematologists and researchers."
>
  <Section variant="gradient-cells" class="hero">
    <div class="text">
      <h1>Cellbytes &ndash; precision hematology with digital microscopy</h1>
      <p>Cellbytes is the colleague you have been waiting for.</p>
      <p>Ready to digitize your hematology workflow? Request a demo to see Cellbytes in action.</p>
    </div>
    <GlassFrame>
      <video
        width="1200"
        autoplay
        muted
        loop
        aria-label="Demonstration of Cellbytes's features"
        poster="banner_image.jpeg"
      >
        <source
          src="banner_large.webm"
          type="video/webm"
          media="(1000px <= width)"
        />
        <source
          src="banner_mid.webm"
          type="video/webm"
          media="(600px <= width < 1000px)"
        />
        <source
          src="banner_small.webm"
          type="video/webm"
          media="(width < 600px)"
        />
      </video>
    </GlassFrame>
  </Section>

  <Section variant="pair">
    <Image
      src={appLabelings}
      alt="Doctor looking at cell labels viewed with Cellbytes."
      widths={WIDTHS}
      height="300"
    />
    <SectionCard>
      <H level="2">Objective and transparent</H>
      <p>
        Cellbytes will not replace hematopathologists. However, standardized and
        real-time digital assessment can enhance human performance and decrease
        subjectivity. Trained on millions of cells across diverse hematologic conditions,
        Cellbytes provides consistent, high-quality outputs. Every model prediction comes with clear visualizations and quantifiable
        data, ensuring transparency and supporting users to review what the software has identified.
      </p>
      <LinkButton href="/application" variant="default-arrow">
        Application
      </LinkButton>
    </SectionCard>
  </Section>

  <Section variant="pair-reverse">
    <Image
      src={appMask}
      alt="Doctor looking at a sample with an overlaid mask viewed with Cellbytes."
      widths={WIDTHS}
      height="300"
    />
    <SectionCard>
      <H level="2">Real-time analysis</H>
      <p>
        Cloud-based computing is available 24/7 and designed for rapid batch analysis.
      </p>
      <LinkButton
        href="/application#vast-suite-of-algorithms"
        variant="default-arrow"
      >
        Algorithms
      </LinkButton>
    </SectionCard>
  </Section>

  <Section variant="gradient">
    <div class="text" style="margin: 4rem;">
      <H level="2">Full AI-assisted morphology analysis</H>
      <p>
        Hematopathology is not just about counting cells. 
        Cellbytes performs end-to-end cytomorphological assessment covering assessment of cellularity,
        sample adequacy, megakaryocyte counts, cell classification, and dysplasia detection.
        Results are summarized in a standardized report that can be seamlessly integrated into your laboratory information system.
      </p>
    </div>
  </Section>

  <Section variant="pair-reverse">
    <Image
      src={microscope}
      alt="Close-up of a microscope scanning a pathological slide."
      widths={WIDTHS}
      height="300"
    />
    <SectionCard>
      <H level="2">Flexible workflow integration</H>
      <p>
        Whole-slide and single-cell image analysis is compatible with diverse microscopes and imaging systems.
        Cellbytes is simple to deploy and scales effortlessly to labs of any size.
        No need for additional hardwares or complex installations &ndash; only your computer.
      </p>
      <LinkButton href="/application#scan-slides" variant="default-arrow">
        Compatibility
      </LinkButton>
    </SectionCard>
  </Section>

  <Section variant="pair">
    <Image
      src={server}
      alt="A rack of servers with blinking lights"
      widths={WIDTHS}
      height="300"
    />
    <SectionCard>
      <H level="2">Safe and secure</H>
      <p>
        Data security is more than a priority; it is a commitment. By strictly
        adhering to international standards and data privacy regulations, we can
        guarantee users to retain have complete control over their data.
      </p>
      <LinkButton href="/compliance" variant="default-arrow">
        Security
      </LinkButton>
    </SectionCard>
  </Section>
</Layout>

<style>
  p {
    font-size: var(--text-size-ingress);
  }

  .hero {
    padding-block: calc(2 * var(--element-padding));
  }

  video {
    aspect-ratio: 16 / 10;
  }

  .algorithms {
    display: flex;
    gap: calc(2 * var(--element-padding));
    flex-wrap: wrap;
    justify-content: center;
  }

  @media screen and (width < 40em) {
    .scroller {
      display: flex;
      gap: calc(var(--element-padding));
      --animation-multiplier: 2;
      padding-inline: calc(2 * var(--element-padding));
      padding-block-end: calc(2 * var(--element-padding));
    }
    .scroller-img {
      min-width: 100%;
    }
  }
</style>
